<div>
    <h2>{{localize (concat (SYSTEMID) ".sheet.actor.perso.titre") }}</h2>
    
        <div class="field">
            <label>{{localizeSystem "sheet.actor.perso.age"}}</label>
            <span>{{ fieldOrInput "system.age" class="pj-sheet-input" }}</span>
        </div>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.perso.situationFamiliale"}}</label>
            <span>{{ fieldOrInput "system.situationFamiliale" class="pj-sheet-input" }}</span>
        </div>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.perso.metier"}}</label>
            <span>{{ fieldOrInput "system.metier" class="pj-sheet-input" }}</span>
        </div>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.perso.loisirs"}}</label>
            <span>{{ fieldOrInput "system.loisirs" class="pj-sheet-input" }}</span>
        </div>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.perso.projet"}}</label>
            <span>{{ fieldOrInput "system.projet" class="pj-sheet-input" }}</span>
        </div>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.perso.valeursMorales"}}</label>
            <span>{{ fieldOrInput "system.valeursMorales" class="pj-sheet-input" }}</span>
        </div>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.perso.secret"}}</label>
            <span>{{ fieldOrInput "system.secret" class="pj-sheet-input" }}</span>
        </div>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.perso.firete"}}</label>
            <span>{{ fieldOrInput "system.firete" class="pj-sheet-input" }}</span>
        </div>


        <div class="field">
            <label>{{localizeSystem "sheet.actor.perso.trauma"}}</label>
            <span>{{ fieldOrInput "system.trauma" class="pj-sheet-input" }}</span>
        </div>

</div>