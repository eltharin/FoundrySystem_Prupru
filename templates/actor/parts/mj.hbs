<div>
    <h2>{{localize (concat (SYSTEMID) ".sheet.actor.mj.titre") }}</h2>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.mj.blessures.legere"}}</label>
            <span>{{ fieldOrInput "system.blessures.legere.max" class="pj-sheet-input" }}</span>
        </div>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.mj.blessures.moyenne"}}</label>
            <span>{{ fieldOrInput "system.blessures.moyenne.max" class="pj-sheet-input" }}</span>
        </div>
        
        <div class="field">
            <label>{{localizeSystem "sheet.actor.mj.blessures.grave"}}</label>
            <span>{{ fieldOrInput "system.blessures.grave.max" class="pj-sheet-input" }}</span>
        </div>

        <div class="field">
            <label>{{localizeSystem "sheet.actor.mj.stress"}}</label>
            <span>{{ fieldOrInput "system.stress.max" class="pj-sheet-input" }}</span>
        </div>
        
        <div class="field">
            <label>{{localizeSystem "sheet.actor.mj.volonte"}}</label>
            <span>{{ fieldOrInput "system.volonte.max" class="pj-sheet-input" }}</span>
        </div>

</div>